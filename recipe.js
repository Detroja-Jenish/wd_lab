var recipe_template = document.getElementById("mainContainer");

const recipes = JSON.parse("recipes.json");

function add_recipe(){
    var image, ingredients, method, heading;
    image = recipe_template.content.querySelector("#image").attribut
}